import {test,expect} from "@playwright/test";import {env} from "../../../core/config/env";import {authenticatedSession} from "../../support/session";
const contracts=[["workspaces","array"],["accounts","array"],["categories","array"],["transactions","array"],["dashboard","object"],["parties","array"],["khata","array"],["loans","array"],["budgets","array"],["savings","array"],["salary","array"],["members","array"],["shared-expenses","array"],["reports/summary","object"],["evidence","array"],["recurring","array"],["preferences","object"]] as const;
test.describe("API | Workspace | Response contracts",()=>{contracts.forEach(([path,type],index)=>test(`API-${String(index+31).padStart(3,"0")} authenticated ${path} contract`,async({request})=>{const {headers,workspace}=await authenticatedSession(request);const route=path==="workspaces"?"/workspaces":`/workspaces/${workspace.id}/${path}`;const r=await request.get(`${env.apiUrl}${route}`,{headers});expect(r.ok()).toBeTruthy();expect(Array.isArray(await r.json())).toBe(type==="array")}))});
