# AI assistant

The assistant is read only and tenant scoped. It answers common income, expense, savings, loan and receivable questions from report data. Responses disclose that no records were changed. A future external model provider can propose actions, but execution must remain behind explicit user confirmation.
